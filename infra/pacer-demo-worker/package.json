{
  "name": "pacer-demo-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker that serves the Pacer AI demo video (Claude-over-Tahoe) so the getpacerai.com v3 homepage can iframe it. The ~160K demo cannot live in a WordPress page.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dryrun": "wrangler deploy --dry-run --outdir dist",
    "tail": "wrangler tail",
    "sync": "./sync.sh",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "^5.4.5",
    "wrangler": "^4.113.0"
  }
}
